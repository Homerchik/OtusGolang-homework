logger:
  level: "DEBUG"
  format: ""

http:
  host: "localhost"
  port: 18080

storage:
  type: sql # possible values [memory, sql]
  sql:
    host: "localhost"
    port: 5432
    dbName: "test"
    driver: "postgres"
    username: "postgres"
    password: "postgres"

